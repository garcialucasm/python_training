# Calculate Hypotenuse
# Read two sides of a square rectangle and calculete the hypotenuse

from math import hypot
op_collar = float(input('Opposite: '))
ad_collar = float(input('Adjacent: '))
hypotenuse = hypot(catop, catad)
print('Hypotenuse: {:.2f}'.format(hypotenuse))
print(type(hypotenuse))